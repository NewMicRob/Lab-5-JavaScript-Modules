// Sets up both strings
const sentence = "Hello, World!";
const sentence2 = "i'm from string";
const sentence3 = "i'm also from the string ";
// Function to uppercase
export function toUpperCase(str) {
    return sentence.toUpperCase();
}

// Function to lowercase
export function toLowerCase(str) {
    return sentence.toLowerCase();
}

// Function to uppercase sentence2
export function toUpperCase2(str) {
    return sentence2.toUpperCase();
}

export function toUpperCase3(str) {
    return sentence3.toUpperCase();
}